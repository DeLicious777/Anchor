{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the mini widget",
  "windows": ["main", "widget"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
