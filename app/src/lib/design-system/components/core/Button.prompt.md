Primary CTA button used across the app; five visual variants for different weights of action.

```jsx
<Button variant="primary" size="md">Start tracking</Button>
<Button variant="secondary">Cancel</Button>
```

Variants: primary (filled anchor), secondary (outlined), ghost (text-only), clay (warm alt action), danger (destructive).
