# Prompts

Reusable prompt fragments shared across agents and workflows. None needed yet — agent system prompts in `.claude/agents/` are currently self-contained. Add fragments here if duplication emerges across agents/workflows/commands (e.g., a shared "how to write an ADR" snippet), rather than copy-pasting.
